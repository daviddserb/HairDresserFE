import { NoWhitespaceAllowedDirective } from './no-whitespace-allowed.directive';

describe('NoWhitespaceAllowedDirective', () => {
  it('should create an instance', () => {
    const directive = new NoWhitespaceAllowedDirective();
    expect(directive).toBeTruthy();
  });
});
